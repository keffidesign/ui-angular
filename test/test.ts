import * as assert from 'assert';
import {Plugin} from 'applugins';
import BaseComponent from '../src/modules/ui/AngularComponent.ts';

console.log('mocha', Plugin);

describe('Parsing JSX to String template', () => {

    it('should be equal', () => {

        assert.equal(1, 1);

    });

});